# สรุปการดำเนินงาน (Walkthrough) - แยกที่อยู่ภูมิลำเนาและเพิ่มฟิลด์ข้อมูลเยาวชน/ผู้ปกครอง

## ปัญหาและหัวข้อการปรับปรุง

### 1. การปรับปรุงช่องกรอกที่อยู่ภูมิลำเนาแบบแยกหัวข้อ
* **ความต้องการ**: ปรับปรุงจากเดิมที่เป็นช่องกรอกแบบข้อความยาวช่องเดียว (Textarea) ให้เป็นแบบแยกกรอกข้อมูลหลายช่องเพื่อการบันทึกฐานข้อมูลที่สมบูรณ์และถูกต้องตามภูมิศาสตร์ สำหรับวิเคราะห์สถิติตามอำเภอ
* **การดำเนินการ**:
  - **ปรับโครงสร้างฐานข้อมูลตาราง `tb_defendants`**: ปรับปรุงฟังก์ชัน `upsertDefendant` ใน [Code.gs](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Code.gs#L1556) โดยเพิ่มกลไก `ensureColumnsExist` ตรวจสอบและสร้างคอลัมน์เก็บข้อมูลแยกส่วน ได้แก่ `number_address` (บ้านเลขที่), `village_address` (หมู่บ้าน), `village_number_address` (หมู่ที่), `road_address` (ถนน), `sub_district_address` (ตำบล/แขวง), `district_address` (อำเภอ/เขต), `province_address` (จังหวัด), และ `postcode_address` (รหัสไปรษณีย์) โดยอัตโนมัติ
  - **ปรับอินเตอร์เฟซการกรอกข้อมูล**:
    - **หน้าแก้ไขประวัติเยาวชน (Edit Modal)** ใน [Index.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Index.html#L1410): ลบช่องกรอกที่อยู่แบบ Textarea เดิมออก เปลี่ยนเป็นแบบฟอร์มตารางกริดที่แบ่งช่องกรอกแยกเป็น 8 ช่องย่อยอย่างสวยงามและเป็นสัดส่วน
    - **แบบฟอร์มเพิ่มเด็กและเยาวชนใหม่ลงระบบเมื่อค้นหาไม่พบ (Register Form)** ใน [Index.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Index.html#L820): เปลี่ยนเป็นแบบแยกกรอกข้อมูล 8 ช่องเช่นเดียวกัน
  - **ปรับปรุงสคริปต์หน้าบ้าน**: เพิ่มตัวแปร Reactive ย่อยใน `defForm` ใน [Scripts.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Scripts.html#L440) เพื่อให้รองรับการผูกข้อมูล (Data Binding) สำหรับส่งค่าผ่าน API
  - **จัดรูปแบบสำหรับแสดงผลที่อยู่ปัจจุบัน (แก้ไขใน V117)**: 
    - ปรับปรุงฟังก์ชัน `formatDefendantAddress` ใน [Code.gs](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Code.gs#L1438) เพื่อนำฟิลด์ย่อยเหล่านี้กลับมารวมเข้าด้วยกันเป็นประโยคที่ถูกต้องตามรูปแบบที่อยู่อย่างเหมาะสม (พร้อมระบบช่วยกรอกคำนำหน้า ต., อ., จ. หรือ เขต/แขวง ให้โดยอัตโนมัติเมื่อค่าที่กรอกมาไม่มีตัวย่ออยู่แล้ว และรองรับกรุงเทพฯ)
    - ปรับปรุงฟังก์ชัน `searchDefendantByCitizenId` และ `getAllDefendantsWithCases` ใน [Code.gs](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Code.gs) ให้เรียกใช้งาน `formatDefendantAddress` เสมอ เพื่อให้การโหลดข้อมูลและค้นหาประวัติได้ที่อยู่ปัจจุบันที่จัดรูปแบบเต็มเรียบร้อย โดยไม่ดึงข้อมูลดิบและป้องกันที่อยู่ขึ้นว่า "ไม่ระบุที่อยู่" บนหน้าการ์ดข้อมูล

### 2. เพิ่มข้อมูลประวัติส่วนตัวเยาวชนและผู้ปกครองเพิ่มเติม (รวมความสัมพันธ์ใน V117)
* **ความต้องการ**: แสดงและแก้ไขฟิลด์: ชื่อผู้ปกครอง (`guardian_name`), ความสัมพันธ์ (`relationship`), เบอร์โทรศัพท์เยาวชน (`phone_number`), เบอร์โทรศัพท์ผู้ปกครอง (`phone_number_guardian`), อาชีพเยาวชน (`occupation`), และอาชีพผู้ปกครอง (`occupation_guardian`)
* **การดำเนินการ**:
  - **หน้าแสดงข้อมูลรายละเอียดเด็ก (Detail Card)** ใน [Index.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Index.html#L727): เพิ่มการแสดงผลข้อมูลทั้ง 6 ฟิลด์นี้ลงในตารางข้อมูลทั่วไป โดยสำหรับความสัมพันธ์ (`relationship`) ได้ดึงมาจัดรูปแบบและแสดงต่อท้ายชื่อผู้ปกครองเป็น `ชื่อผู้ปกครอง (ความสัมพันธ์)` เช่น `นางสาวปรียา ดีใจ (มารดา)`
  - **การกรอกและแก้ไขข้อมูล**: เพิ่มอินพุตฟิลด์ "เกี่ยวข้องเป็น" (`relationship`) คู่กับชื่อผู้ปกครอง ลงในทั้ง **แบบฟอร์มเพิ่มเยาวชนรายใหม่** และ **หน้าต่างแก้ไขประวัติ (Edit Modal)** ใน [Index.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Index.html) ถัดจากช่องชื่อผู้ปกครอง
  - **ตัวเก็บข้อมูลฝั่งเซิร์ฟเวอร์**: มั่นใจว่าฟิลด์เหล่านี้รวมถึง `relationship` ซิงค์และเขียนลงตารางชีท `tb_defendants` อย่างครบถ้วน

### 3. ปรับเปลี่ยนป้ายข้อความประวัติ และเปิดให้แก้ไขและบันทึกประวัติเพิ่มเติม (แก้ไขใน V118)
* **ความต้องการ**: 
  - เปลี่ยนชื่อป้ายข้อความจาก "ประวัติการรักษาแพทย์" เป็น **"ประวัติการรักษา"** (เชื่อมโยงกับ `medical_history`)
  - เปลี่ยนชื่อป้ายข้อความจาก "ประวัติการยุ่งเกี่ยวสารเสพติด" เป็น **"ประวัติการใช้สารเสพติด"** (เชื่อมโยงกับ `drug_history`)
  - เพิ่มการแสดงผลและการแก้ไขข้อมูล **"ความสนใจ/ความสามารถพิเศษ"** (เชื่อมโยงกับ `interested`)
  - เพิ่มช่องอินพุตข้อมูลทั้ง 3 รายการนี้ในส่วนการลงทะเบียนประวัติและหน้าต่างแก้ไขประวัติเยาวชน
* **การดำเนินการ**:
  - **การปรับเปลี่ยนใน Index.html**:
    - อัปเดตป้ายแสดงผลในประวัติเด็กหลักและหน้ารายละเอียดคัดกรองจากหน้าต่างปฏิทิน (`calendarDetailDef`)
    - เพิ่มช่อง Textarea 3 ช่องใหม่สำหรับประวัติการรักษา, ประวัติการใช้สารเสพติด, และความสนใจ/ความสามารถพิเศษ ในทั้งแบบฟอร์มเพิ่มประวัติใหม่และหน้าต่างแก้ไขประวัติเยาวชน
  - **ตัวตรวจสอบคอลัมน์ฝั่งเซิร์ฟเวอร์ (Code.gs)**: เพิ่ม `medical_history`, `drug_history`, และ `interested` ลงในรายการตรวจสอบ `ensureColumnsExist` เพื่อรองรับการสร้างหัวตารางในสเปรดชีตอย่างปลอดภัยเมื่อมีการลงทะเบียนเด็กใหม่

### 4. เพิ่มข้อมูลประวัติการศึกษา ปรับคำอธิบายที่อยู่ จัดเรียงข้อมูลเด็กไว้บน ผู้ปกครองไว้ล่าง (แก้ไขใน V119)
* **ความต้องการ**:
  - เพิ่มและเปิดให้แก้ไขข้อมูล **"ระดับการศึกษา"** (`education_level`) และ **"โรงเรียน/วิทยาลัย"** (`school`)
  - จัดเรียงลำดับหัวข้อการแสดงผลและการกรอกข้อมูลใหม่ โดยข้อมูลทุกอย่างของเยาวชนจะอยู่ **ส่วนบนสุด** ทั้งหมด และข้อมูลของผู้ปกครองย้ายไปแสดงที่ **ล่างสุด**
  - ในหน้าแก้ไขข้อมูล ปรับป้ายกำกับส่วนที่อยู่แยกช่องจากเดิม "ที่อยู่ภูมิลำเนา (เพื่อประมวลผลคำนวณสถิติอำเภอ)" เป็น **"ที่อยู่ปัจจุบันของเด็ก/เยาวชน"**
* **การดำเนินการ**:
  - **การจัดเรียงและการแสดงผล**: ปรับการเรนเดอร์ใน [Index.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Index.html) (หน้ารายละเอียดหลัก, ฟอร์มลงทะเบียนใหม่, หน้าต่างแก้ไขประวัติ, และหน้าปฏิทินนัดหมาย) เพื่อเรียงลำดับฟิลด์ของเยาวชนไว้ข้างบน (มีระดับการศึกษาและโรงเรียนเพิ่มเข้ามา) และนำส่วนการกรอก/แสดงผล ข้อมูลผู้ปกครอง (ชื่อผู้ปกครอง, ความเกี่ยวข้อง, เบอร์โทรศัพท์, อาชีพผู้ปกครอง) ย้ายไปอยู่ในกล่องการ์ดด้านล่างสุดของฟอร์ม
  - **การอัปเดตเซิร์ฟเวอร์และโมเดล**:
    - เพิ่มการตรวจสอบคอลัมน์ `education_level` และ `school` ในฟังก์ชัน `ensureColumnsExist` ของ [Code.gs](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Code.gs)
    - ผูกค่า `defForm.school` และ `defForm.education_level` เพื่อส่งไปบันทึกฝั่งเซิร์ฟเวอร์ใน [Scripts.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Scripts.html)

### 5. ปรับเปลี่ยนหัวข้อผลประเมินทางจิตวิทยาเบื้องต้น เป็น การประเมินความเสี่ยงในการกระทำผิดซ้ำ (แก้ไขใน V120)
* **ความต้องการ**:
  - เปลี่ยนชื่อป้ายข้อความจาก "ผลการตรวจวินิจฉัยทางจิตวิทยาเบื้องต้น" เป็น **"การประเมินความเสี่ยงในการกระทำผิดซ้ำ"** (เชื่อมโยงกับ `risk_assessment`)
  - ปรับรูปภาพสัญลักษณ์ (Icon) ข้างหน้าข้อความให้สื่อถึงการประเมินความเสี่ยงเชิงปริมาณ (ใช้ไอคอนมาตรวัดความเร็ว/ความเสี่ยง `fa-gauge-high`)
  - ในหน้าบันทึกและแก้ไขข้อมูล ปรับเปลี่ยนจากช่องใส่ข้อความฟรีสไตล์ (Textarea) เป็นช่อง Dropdown รายการตัวเลือกแบบสวยงาม
* **การดำเนินการ**:
  - **การแสดงผลและการ์ดสรุปประวัติ**: อัปเดตใน [Index.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Index.html) ให้แสดงแถบสีข้อความ (Badge/Tag) ตามระดับความเสี่ยง of เยาวชนที่ได้รับบันทึกโดยอัตโนมัติ (เช่น ความเสี่ยงสูงจะใช้สีแดงอ่อน, ความเสี่ยงต่ำใช้สีเขียวอ่อน)
  - **การอัปเดตสิทธิ์และตาราง**:
    - เพิ่มการยืนยันคอลัมน์ `risk_assessment` ในตาราง `tb_defendants` ผ่านฟังก์ชัน `ensureColumnsExist` ของ [Code.gs](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Code.gs)
    - ผูกตัวเลือกในกล่อง Dropdown หน้าแก้ไขและหน้าลงทะเบียนเด็กเข้ากับพร็อพเพอร์ตี้ `defForm.risk_assessment` ใน [Scripts.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Scripts.html) พร้อมกำหนดค่าดีฟอลต์เป็นระดับความเสี่ยงต่ำ

### 6. แยกการ์ดข้อมูลผู้ปกครอง และอัปเดตป้ายข้อความแสดงผลและกรอกข้อมูล (แก้ไขใน V121)
* **ความต้องการ**:
  - ในหน้าแสดงผลประวัติเด็ก แยกหัวข้อ "ชื่อผู้ปกครอง", "เบอร์โทรศัพท์ผู้ปกครอง", และ "อาชีพผู้ปกครอง" ออกมาเป็น **อีกการ์ดหนึ่งแยกต่างหาก** อย่างเป็นสัดส่วนชัดเจน
  - เปลี่ยนชื่อป้ายกำกับฟิลด์ (ทั้งในตารางแสดงผลและแบบฟอร์มกรอกข้อมูล):
    - `ชื่อ-สกุลจริง:` เปลี่ยนเป็น **`ชื่อ-สกุล (ชื่อเล่น/ฉายา):`**
    - `เบอร์โทรศัพท์เยาวชน:` เปลี่ยนเป็น **`เบอร์โทรศัพท์:`**
    - `อาชีพเยาวชน:` เปลี่ยนเป็น **`อาชีพ (ตำแหน่ง):`**
    - `อาชีพผู้ปกครอง:` เปลี่ยนเป็น **`อาชีพผู้ปกครอง (ตำแหน่ง):`**
* **การดำเนินการ**:
  - **ปรับโครงสร้างอินเตอร์เฟซ (Index.html)**:
    - ปรับโครงสร้างฝั่งซ้ายของหน้ารายละเอียดเด็กโดยสร้าง Layout Stacking ครอบการ์ด 2 ใบ: การ์ดแรกแสดงเฉพาะรายละเอียดเยาวชนล้วน ๆ และเพิ่มการ์ดใบที่สองแยกแสดงรายละเอียดข้อมูลผู้ปกครอง (ใช้สัญลักษณ์ `fa-people-roof`)
    - อัปเดตคำแสดงผลในฟอร์มกรอกประวัติใหม่และหน้าแก้ไขข้อมูลผู้มีสิทธิ์ให้ตรงตามป้ายข้อความที่ระบุใหม่เรียบร้อยแล้ว

### 7. เพิ่มการ์ดความเห็นเพิ่มเติมของผู้ให้คำปรึกษาพร้อมระบบบันทึกทันที (แก้ไขใน V122)
* **ความต้องการ**:
  - เพิ่มกล่องการ์ด **"ความเห็นเพิ่มเติมของผู้ให้คำปรึกษาที่มีต่อเด็กและเยาวชน"** นำไปวางไว้ใต้การ์ดข้อมูลผู้ปกครอง
  - สามารถป้อนข้อมูลความคิดเห็นและคลิกบันทึกได้ทันทีจากหน้าจอแสดงผลประวัติโดยไม่ต้องเข้าหน้าแก้ไขข้อมูลหลัก
  - จำกัดสิทธิ์การแก้ไข/บันทึกความเห็นเฉพาะผู้ใช้บทบาท: **ผู้ให้คำปรึกษา (counselor)**, **นักจิตวิทยา (psychologist)**, และ**แอดมิน (admin)** เท่านั้น
* **การดำเนินการ**:
  - **การปรับฐานข้อมูล (Code.gs)**: เพิ่มการตรวจสร้างคอลัมน์ `counselor_comments` อัตโนมัติ และสร้างฟังก์ชันความปลอดภัย `updateCounselorComments` บนเซิร์ฟเวอร์เพื่อเช็คเซสชันและบทบาทผู้เขียน
  - **การ์ดและปุ่มบันทึกด่วน (Index.html)**: นำการ์ดใบที่สามมาซ้อนต่อท้ายฝั่งซ้าย โดยจะประเมินสิทธิ์ของผู้ใช้ปัจจุบัน
  - **การผูกฟังก์ชัน JavaScript (Scripts.html)**: ผูกฟังก์ชัน `saveCounselorComments` เข้ากับ API ของระบบพร้อมแสดง SweetAlert2 เมื่อบันทึกสำเร็จโดยไม่มีการรีโหลดหน้าเว็บ

### 8. ปรับปรุงความสม่ำเสมอของสีการ์ดความเห็นเพิ่มเติม และเชื่อมโยงข้อมูลกับคอลัมน์ `counselor_note` (แก้ไขใน V123-128)
* **ความต้องการ**:
  - ปรับปรุงสีและการออกแบบของการ์ด "ความเห็นเพิ่มเติมของผู้ให้คำปรึกษาฯ" เพื่อให้กลมกลืนและเป็นหนึ่งเดียวกับการ์ดประวัติเยาวชนและการ์ดข้อมูลผู้ปกครองอื่น ๆ (ไม่มีการฝืนสีพื้นหลังหรือเส้นขอบ override)
  - เปลี่ยนการเชื่อมโยงข้อมูลไปที่คอลัมน์ **`counselor_note`** ในตาราง `tb_defendants` แทนคอลัมน์ `note` เดิม
  - อนุญาตให้ผู้มีสิทธิ์ (แอดมิน, นักจิตวิทยา, และผู้ให้คำปรึกษา) สามารถเขียนและบันทึกข้อมูลได้
  - สำหรับบทบาท **ผู้อำนวยการศูนย์ (director)** ให้สามารถ **อ่านข้อมูลได้อย่างเดียว** ไม่สามารถแก้ไขหรือบันทึกได้
  - **แก้ไขการทำงานของปุ่มบันทึกและการผูกฟังก์ชัน (แก้ไขใน V128)**: 
    - จัดสิทธิ์แบบฟอร์มและการผูกฟังก์ชันหน้าบ้าน ให้ใช้งานตัวแปร `tempCounselorNote` ร่วมกับฟังก์ชัน `startEditCounselorNote` และ `cancelEditCounselorNote` ตรงกันระหว่างไฟล์ `Index.html` และ `Scripts.html`
    - แก้ไขฟังก์ชันการยิง API `updateCounselorComments` ใน `Scripts.html` โดยส่งตัวแปรยืนยันตัวตน `session.value.token` ไปทางฝั่งหลังบ้านอย่างสมบูรณ์เพื่อป้องกัน Error การหมดอายุของสิทธิ์
    - ปรับปรุงกลไกตรวจสอบเลขบัตรประชาชนใน `Code.gs` ให้ใช้ฟังก์ชัน `cleanCitizenId` ทั้งสองฝั่งเทียบเคียงกันอย่างแม่นยำ ไม่ติดข้อจำกัดของเครื่องหมายขีดคั่นคดี
    - ทำแถบปุ่มแก้ไขและบันทึกความเห็น: แบบปกติแสดงปุ่มสีน้ำเงิน "แก้ไขความเห็น", แบบแก้ไขแสดงปุ่มแดง "ยกเลิก" และปุ่มเขียว "บันทึกความเห็น" ครบถ้วนตามโจทย์

### 9. ปรับปรุงตำแหน่งและสไตล์ของหน้าจอประมวลผลดึงข้อมูล (loading-overlay) (แก้ไขใน V130)
* **ความต้องการ**: 
  - นำข้อความและตัวสปินเนอร์ที่แสดงขณะประมวลผลหรือโหลดดึงข้อมูลใหม่ ไปแสดงผลตรงกลางส่วนบนสุดของหน้าจอ เพื่อความทันสมัยและสวยงามตามหลักสรีระสายตาบนหน้าจอดิจิทัลทุกอุปกรณ์ (Responsive design)
* **การดำเนินการ**:
  - ปรับปรุงสไตล์ชีท `.loading-overlay` ใน [Styles.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Styles.html#L1058) ให้เปลี่ยนการจัดตําแหน่งจาก Absolute ของคอมโพเนนต์ย่อยเป็น **`position: fixed`** ครอบคลุมพื้นที่แสดงผลของบราวเซอร์ทั้งหมด (`100vw`, `100vh`) พร้อมการเบลอหลังฉาก (Glassmorphism backdrop filter 8px) เพื่อสกัดการคลิกอื่นในระหว่างโหลดข้อมูล
  - จัดการแสดงผลตัวสปินเนอร์และข้อความโดยเปลี่ยนมาใช้ `justify-content: flex-start` ร่วมกับ `padding-top: 40px` เพื่อดันข้อความแจ้งเตือนสถานะขึ้นไปอยู่บริเวณ **กึ่งกลางขอบบนสุดของหน้าจอ** ทุกขนาดหน้าจออุปกรณ์มือถือ แท็บเล็ต และคอมพิวเตอร์อย่างเป็นระเบียบเรียบร้อย

### 10. ปรับโครงสร้างแดชบอร์ด จัดคอลัมน์ใหม่ และเพิ่มการ์ดคดีคงค้างผู้ให้คำปรึกษา (แก้ไขใน V132)
* **ความต้องการ**:
  - เปลี่ยนหัวข้อ "สัดส่วนจำแนกตามเพศ" เป็น **"จำแนกตามเพศ"**
  - ปรับโครงสร้างของ Demographics Grid ในหน้าแดชบอร์ด ให้เป็นตารางกริด 2 คอลัมน์ที่สมดุลและจัดกลุ่มความสัมพันธ์อย่างสวยงาม
  - ย้ายหัวข้อ "จำแนกตามช่วงอายุขณะกระทำผิด" ไปต่อท้ายขยับลงมาอยู่ด้านใต้ของ "จำแนกตามเพศ" (ฝั่งซ้ายของแดชบอร์ด)
  - เลื่อนส่วน "จำแนกตามอำเภอที่อยู่อาศัย (จ.ระยอง)" ไปทางด้านบนฝั่งขวาแทนตำแหน่งเดิมของสถิติตามช่วงอายุ
  - เพิ่ม **การ์ดใหม่ "คดีคงค้างของผู้ให้คำปรึกษา"** ที่มุมขวาล่างสุด แสดงเฉพาะข้อมูลผู้มีบทบาท `ผู้ให้คำปรึกษา` (จาก `tb_counselor` คอลัมน์ `counselor_position`) ที่มีคดีคงค้างที่ยังไม่ยุติ ณ ปัจจุบัน (พิจารณาจากกรณีที่คอลัมน์ `red_case_no` ในตาราง `tb_cases` ยังไม่มีการระบุหรือมีเลขแดง) เรียงลำดับจากคนที่มีคดีค้างมากที่สุดไปน้อยที่สุด และซ่อนผู้ที่ไม่มีคดีค้าง
* **การดำเนินการ**:
  - **ฝั่งหลังบ้าน (Code.gs)**: ปรับปรุงฟังก์ชัน `getDashboardData` โดยดึงตาราง `tb_counselor` มาคัดกรองเฉพาะตำแหน่ง *"ผู้ให้คำปรึกษา"* จากนั้นนำมาประมวลผลร่วมกับตาราง `tb_cases` เพื่อนับคดีคงค้าง (ไม่มีเลขแดง) ของแต่ละบุคคล จัดกลุ่มเป็น Array วัตถุ และเรียงลำดับแบบ Descending ก่อนส่งกลับมาหน้าบ้าน
  - **ฝั่งสคริปต์หน้าบ้าน (Scripts.html)**: เพิ่มตัวแปร Reactive `counselorActiveCases` ในโมเดลสถิติหลัก และอัปเดตสคริปต์ `updateDashboardUI` และข้อมูลจำลองการออฟไลน์ให้ผูกเชื่อมโยงข้อมูลอย่างครบถ้วน
  - **การแสดงผล (Index.html)**: จัดโครงสร้าง HTML ในการ์ด Demographics ใหม่เป็นกริด 2 คอลัมน์ โดยแบ่งฝั่งซ้ายและฝั่งขวาอย่างชัดเจนเพื่อสแต็คข้อมูลตามที่กำหนด พร้อมออกแบบการ์ดคดีคงค้างผู้ให้คำปรึกษาด้วยแถบเปอร์เซ็นต์ความคืบหน้า (Progress bar) สีน้ำเงินม่วงไล่เฉด สอดคล้องกับธีมหลักของระบบ

### 11. แยกสถิติประชากรศาสตร์และรายชื่อผู้ดูแลออกจากกันเป็น 2 การ์ดต่างหาก (แก้ไขใน V133)
* **ความต้องการ**:
  - แยกข้อมูลสถิติออกเป็น 2 การ์ดหลักวางคู่ขนานกัน:
    - **การ์ดฝั่งซ้าย**: "จำแนกสถิติเด็กและเยาวชนรับเข้าใหม่ (เพศ, ช่วงอายุ, และรายอำเภอในระยอง)" (มี Gender, Age, District อยู่ภายใน)
    - **การ์ดฝั่งขวา**: เปลี่ยนชื่อใหม่เป็น **"จำนวนเด็กและเยาวชนที่อยู่ในความดูแลของผู้ให้คำปรึกษา"** (แสดงรายชื่อผู้ให้คำปรึกษาและคดีในความดูแล)
  - แบ่งการ์ดออกจากกันให้ชัดเจน ปรับขนาดความกว้างและความสูงให้เหมาะสม สวยงาม และสอดคล้องกับการ์ดด้านบน
  - ปรับการนับและตัวอักษรต่อท้ายจาก "คดี" ให้เป็นหน่วย **"คน"** เพื่อความสอดคล้องกับหัวข้อการดูแลเยาวชน
* **การดำเนินการ**:
  - **การปรับ Layout (Index.html)**: ใช้ Wrapper Grid ของแดชบอร์ดที่มีอัตราส่วนความกว้าง 2fr ต่อ 1fr เพื่อแบ่งการ์ดที่ 1 (ซ้าย: สถิติเด็กและเยาวชน) และการ์ดที่ 2 (ขวา: รายชื่อผู้ให้คำปรึกษาและจำนวนคดีคงค้าง) ออกจากการ์ดใบเดียวกันเดิมอย่างสมบูรณ์ ทำให้ทั้งสองการ์ดสูงเท่ากันและสอดรับกับสัดส่วน Layout ของการ์ดนัดหมายกับกราฟข้อหาด้านบน
  - **การแก้ไขข้อความใน Index.html**: เปลี่ยนป้ายชื่อการ์ดเป็น *"จำนวนเด็กและเยาวชนที่อยู่ในความดูแลของผู้ให้คำปรึกษา"* และแก้ไขค่าแสดงผลหน่วยนับจาก `{{ item.count }} คดี` เป็น `{{ item.count }} คน`

### 12. นับจำนวนเยาวชนคงค้างแบบไม่นับซ้ำ และเพิ่มรายละเอียดป๊อปอัปรายบุคคล (แก้ไขใน V134)
* **ความต้องการ**:
  - **การนับไม่ซ้ำ**: หากพบว่าเยาวชนรายเดียวกัน (เลขบัตรประชาชน `citizen_id` ตรงกัน) มีคดีคงค้างหลายคดีในคนเดียว ให้นับเป็นเพียง 1 คนเท่านั้น เพื่อสะท้อนยอด "คน" ที่ดูแลจริง
  - **ป๊อปอัปรายละเอียด**: เมื่อคลิกที่ชื่อผู้ให้คำปรึกษา ให้เปิดป๊อปอัปแสดงรายละเอียดของเยาวชนในการดูแลของท่านนั้น ๆ โดยต้องแสดง เลขคดี, ข้อหา และวันนัดครั้งต่อไปของทุกคดีที่สอดคล้องกับเยาวชนแต่ละคนโดยไม่ตกหล่น (ห้ามตัดคดีอื่นออกหากเยาวชน 1 คนมีหลายคดี)
* **การดำเนินการ**:
  - **ปรับปรุงการคำนวณฝั่งหลังบ้าน (Code.gs)**: อัปเดตส่วนนับยอดคงค้างใน `getDashboardData` จากที่นับยอดแถวคดี มาเป็นเก็บ `citizen_id` ในออบเจกต์ Map/Set เพื่อกรองและนับเฉพาะเด็กที่ไม่ซ้ำกันจริง ๆ
  - **พัฒนาฟังก์ชันดึงรายละเอียดด่วน (Code.gs)**: สร้างฟังก์ชัน `getCounselorActiveCasesDetail` เพื่อเชื่อมโยงคดีใน `tb_cases` เข้ากับรายละเอียดเด็กใน `tb_defendants` และค้นหาประวัติวันนัดหมายครั้งถัดไปที่ใกล้ที่สุด (วันนัด >= วันปัจจุบัน) จาก `tb_appointment`
  - **ปรับอินเตอร์เฟซและฟังก์ชันป๊อปอัป (Index.html & Scripts.html)**:
    - ปรับปรุงการ์ดรายชื่อผู้ให้คำปรึกษา โดยทำให้ชื่อมีขีดเส้นใต้พร้อมปุ่มลิงก์และลูกเล่นเลื่อนเมาส์ผ่าน (Hover translation)
    - ออกแบบโมเดลป๊อปอัปแบบกระจายการ์ดรายบุคคล (Card layout) ด้านในแสดงข้อมูลเยาวชนและคดีที่สอดคล้อง โดยมีกลไก Responsive ป้องกันการล้นจอและจัดเรียงข้อมูลแบบเรียบง่าย

### 13. ปรับเปลี่ยนข้อมูลแสดงผลในป๊อปอัปให้แสดงเฉพาะชื่อเล่นและซ่อนเลขบัตรประชาชน (แก้ไขใน V135)
* **ความต้องการ**:
  - **แสดงเฉพาะชื่อเล่น**: ในส่วนของชื่อ - นามสกุลในป๊อปอัปรายละเอียดเยาวชน ให้เปลี่ยนไปดึงค่าจากคอลัมน์ `nick_name` ในตาราง `tb_defendants` มาแสดงผลเท่านั้น หากเยาวชนรายใดไม่มีชื่อเล่นในตารางประวัติ ก็ไม่ต้องแสดงผลแถบชื่อ
  - **ซ่อนเลขบัตรประชาชน**: ลบกล่องและข้อความแสดงผลเลขประจำตัวประชาชน (citizen_id) ออกทั้งหมดเพื่อความปลอดภัยและความเป็นส่วนตัว
  - **ปรับแต่งข้อความสรุปยอด**: เปลี่ยนชื่อป้ายสรุปยอดจำนวนคนจากเดิม *"รวมเยาวชนในการดูแลทั้งหมด (ไม่นับซ้ำรายชื่อ):"* เป็น **`"รวมเยาวชนในการดูแลทั้งหมด:"`**
* **การดำเนินการ**:
  - **การจัดการฝั่งหลังบ้าน (Code.gs)**: ปรับปรุงฟังก์ชัน `getCounselorActiveCasesDetail` ให้หยุดการคอมไพล์ชื่อ-สกุลเต็มเยาวชน และเปลี่ยนเป็นดึงเฉพาะฟิลด์ `nick_name` ส่งกลับมาในพร็อพเพอร์ตี้ `nickName`
  - **การปรับแต่งฝั่งหน้าบ้าน (Index.html & Scripts.html)**:
    - ปรับปรุงแถบหัวข้อข้อมูลเยาวชนในแต่ละกล่องให้แสดงเฉพาะ `youth.nickName` และควบคุมการเรนเดอร์แถบด้วย `v-if="youth.nickName"` ทำให้หากไม่มีชื่อเล่น แถบหัวข้อดังกล่าวจะถูกซ่อนตัวไปโดยอัตโนมัติ
    - เอาโค้ดป้ายเลขบัตรประชาชนออกทั้งหมด
    - อัปเดตข้อความหัวสรุปยอดเป็น *"รวมเยาวชนในการดูแลทั้งหมด:"*

### 14. เพิ่มข้อมูลสาเหตุการนัดหมายและเวลานัดหมายในป๊อปอัป (แก้ไขใน V136)
* **ความต้องการ**:
  - ในส่วนของวันนัดครั้งต่อไป ให้ดึงและแสดงข้อมูล **สาเหตุที่นัดหมาย** จากคอลัมน์ `appointment_reason`
  - ดึงข้อมูล **เวลาที่นัดหมาย** จากคอลัมน์ `appointment_time` พร้อมจัดแต่งการแสดงผลให้อยู่ในรูปแบบ **"xx.xx น."** เสมอ
* **การดำเนินการ**:
  - **ปรับปรุงการดึงค่า (Code.gs)**: ในส่วนวนลูปค้นหาการนัดหมายครั้งถัดไปของฟังก์ชัน `getCounselorActiveCasesDetail` เพิ่มการดึงค่า `appointment_reason` และ `appointment_time` สำหรับเหตุการณ์นัดหมายที่ตรงเงื่อนไข (วันนัด >= วันปัจจุบัน)
  - **จัดโครงสร้างแสดงผลและการกรองหน้าบ้าน (Index.html & Scripts.html)**:
    - เพิ่มแถวข้อมูลเวลาต่อท้ายวันนัดโดยเรียกใช้งานฟังก์ชัน `formatAppointmentTime` เพื่อแปลงจากรูปแบบเวลามาตรฐาน ("hh:mm:ss") หรือทศนิยมให้เป็นรูปแบบ "xx.xx น." อย่างถูกต้องสม่ำเสมอ
    - เพิ่มบล็อกข้อความแสดง *"สาเหตุที่นัด: [ข้อความ]"* ด้านล่างวันนัดในป๊อปอัป โดยจะแสดงก็ต่อเมื่อมีนัดหมายในอนาคตเท่านั้น

### 15. ปรับตำแหน่งการแสดงผล "สาเหตุที่นัด" ไปฝั่งขวาใต้ "วันนัด" (แก้ไขใน V137)
* **ความต้องการ**:
  - ย้ายข้อมูล **สาเหตุที่นัด** ไปจัดแสดงที่ **ด้านขวา** ของการ์ด และจัดเรียงให้อยู่ **ใต้ข้อมูลวันนัด**
* **การดำเนินการ**:
  - **การจัดการฝั่งหน้าบ้าน (Index.html)**:
    - ปรับโครงสร้าง Flexbox ของแถวรายละเอียดคดี โดยเปลี่ยน `align-items` จาก `center` เป็น `flex-start` เพื่อความสมมาตรเมื่อคอนเทนต์มีหลายบรรทัด
    - รวบกลุ่มข้อมูลวันนัด (วันนัด + เวลา) และข้อมูลสาเหตุที่นัด เข้าไปในคอนเทนเนอร์เดียวกันทางด้านขวามือ โดยใช้ `display: flex; flex-direction: column; align-items: flex-end; text-align: right;` เพื่อจัดข้อความและเหตุผลการนัดหมายทั้งหมดให้ชิดขวาอย่างเรียบร้อยและสวยงาม

### 16. จัดเรียงวันนัดและสาเหตุที่ใกล้มาถึงวันนัดหมายมากที่สุดก่อนเสมอ (แก้ไขใน V138)
* **ความต้องการ**:
  - เรียงลำดับข้อมูลนัดหมายให้แสดงรายการที่ใกล้จะถึงวันนัดหมายมากที่สุดขึ้นก่อน โดยเทียบเคียงจากวันปัจจุบัน ณ เครื่องผู้ใช้งาน
* **การดำเนินการ**:
  - **การจัดการฝั่งหน้าบ้าน (Scripts.html)**:
    - สร้างฟังก์ชัน `sortYouthActiveCases` เพื่อประมวลผลจัดเรียงอาร์เรย์ `youthList` และประวัติคดีความ/นัดหมายสะสม
    - **การจัดเรียงระดับที่ 1 (ภายในรายบุคคล)**: นำคดีความแต่ละคดีของเด็กรายนั้น ๆ มาเรียงลำดับตามความใกล้ของเวลานัดหมาย (คดีใดที่มีนัดหมายในอนาคตใกล้สุดจะขึ้นก่อน และคดีที่ระบุเป็น "ไม่มีนัดหมายในอนาคต" จะถูกปัดไปอยู่ท้ายสุดของรายการ)
    - **การจัดเรียงระดับที่ 2 (ระดับกล่องเด็ก)**: นำกลุ่มรายชื่อเยาวชนมาจัดลำดับใหม่ทั้งหมดในป๊อปอัป โดยเด็กที่มีการนัดหมายที่ใกล้มาถึงมากที่สุดจะแสดงอยู่ด้านบนสุดของรายการ

### 17. กรองเยาวชนคงค้างเฉพาะที่มีวันนัดหมายในอนาคตและเปลี่ยนชื่อการ์ด (แก้ไขใน V139)
* **ความต้องการ**:
  - กรองรายชื่อเยาวชนในความดูแลของผู้ให้คำปรึกษา โดยคัดออกเฉพาะคดีที่ไม่มีวันนัดหมายในอนาคต (ไม่ต้องนำมาคำนวณหรือแสดงผลบนแดชบอร์ด/ป๊อปอัปเลย)
  - ปรับชื่อการ์ดแดชบอร์ดด้านขวาจากเดิม เป็น *"จำนวนเด็กและเยาวชนที่อยู่ในความดูแลของผู้ให้คำปรึกษา ณ ปัจจุบัน"*
* **การดำเนินการ**:
  - **การจัดการฝั่งหลังบ้าน (Code.gs)**: ปรับปรุง `getDashboardData` และ `getCounselorActiveCasesDetail` ให้กรองเช็คกับตาราง `tb_appointment` เฉพาะคดีที่มีวันนัด >= วันปัจจุบัน ถึงจะนำมาคำนวณและส่งคืนข้อมูล
  - **การจัดการฝั่งหน้าบ้าน (Index.html & Scripts.html)**: อัปเดตส่วนหัวการ์ดแดชบอร์ดด้านขวา และฟังก์ชันเรียงลำดับให้กรองรายการคดีที่หมดนัดหมายออกอย่างสมบูรณ์

### 18. ปุ่มกดค้างเพื่อแสดงรหัสผ่านและฟีเจอร์ลืมรหัสผ่าน (แก้ไขใน V140)
* **ความต้องการ**:
  - เพิ่มสัญลักษณ์รูปตาทางขวาสุดของช่องกรอกรหัสผ่านในหน้าล็อกอิน เมื่อกดค้างจะแสดงรหัสผ่าน และเมื่อปล่อยเมาส์/เลิกกดจะซ่อนรหัสผ่านตามเดิม
  - เพิ่มฟังก์ชันและหน้าต่างสำหรับ "ลืมรหัสผ่าน" ที่แสดงช่องให้กรอกชื่อผู้ใช้/อีเมล และรหัสผ่านใหม่ เพื่อทำการรีเซ็ทรหัสผ่านโดยตรงอย่างสะดวกและปลอดภัย
* **การดำเนินการ**:
  - **การจัดการฝั่งหน้าบ้าน (Index.html & Scripts.html)**:
    - เพิ่มปุ่มไอคอนรูปตาทางขวาสุดในช่องรหัสผ่านของฟอร์มล็อกอิน, สมัครสมาชิก, และรีเซ็ทรหัสผ่านใหม่ โดยผูกอีเวนต์เมาส์ (`mousedown`, `mouseup`, `mouseleave`) และการสัมผัสของมือถือ (`touchstart`, `touchend`, `touchcancel`) เข้ากับค่าตัวแปร `showPasswordLogin`, `showPasswordRegister`, และ `showPasswordForgot` เพื่อให้แสดงรหัสผ่านเฉพาะตอนกดค้าง
    - ออกแบบและสร้างอินเตอร์เฟซฟอร์ม "ลืมรหัสผ่าน" (`authView === 'forgot'`) เพื่อรองรับการกรอกชื่อผู้ใช้/อีเมล และการระบุรหัสผ่านใหม่ที่ต้องการอัปเดต พร้อมปุ่มยืนยันและลิงก์กลับหน้าเข้าสู่ระบบอย่างครบถ้วน
  - **การจัดการฝั่งหลังบ้าน (Code.gs)**: พัฒนาฟังก์ชัน `resetUserPassword` ค้นหาข้อมูลผู้ใช้งานในตาราง `tb_user` ด้วยอีเมลหรือชื่อผู้ใช้งาน หากพบจะนำมาคำนวณแปลงรหัสผ่านใหม่ด้วยเกลือ (Salt) และทำการอัปเดตค่ารหัสผ่านใหม่ลงชีททันที


### 19. ปรับปรุงการแสดงสถิติประเภทคดีและข้อมูลเพศ (แก้ไขใน V141)
* **ความต้องการ**:
  - **สถิติประเภทคดี**: ปรับจากแสดงผลแค่สูงสุด 10 อันดับแรก เป็นแสดงผล **ทุกประเภทคดี** บนกราฟวงกลม/โดนัท โดยจัดเรียงจากประเภทคดีที่มากที่สุดไปน้อยที่สุด และหากจำนวนประเภทคดีเท่ากัน ให้จัดอยู่ในลำดับ (Rank) เดียวกัน แสดงข้อความชิดขวาในรูปแบบ: `(ลำดับที่).(ข้อหาที่ 1) (จำนวนคดีที่ 1), (ข้อหาที่ 2) (จำนวนคดีที่ 2)...`
  - **สถิติจำแนกตามเพศ**: เพิ่มกลุ่มข้อมูล **"ไม่มีข้อมูล"** เข้าไปด้วย สำหรับข้อมูลเคสที่ไม่สามารถระบุได้ว่าเป็นชายหรือหญิง
* **การดำเนินการ**:
  - **การจัดการฝั่งหลังบ้าน (Code.gs)**:
    - ใน `getDashboardData` — ยกเลิกการจำกัด slice(0, 10) ของประเภทข้อหา เพื่อส่งกลับข้อมูลความถี่ของข้อหาทั้งหมด
    - เพิ่มค่าเริ่มต้น `"ไม่มีข้อมูล": 0` ให้กับ `genderCountsBySection` และปลดล็อกเงื่อนไขที่ข้ามการนับเพื่อให้บันทึกสถิติเคสที่ข้อมูลไม่ครบถ้วนได้
  - **การจัดการฝั่งหน้าบ้าน (Index.html & Scripts.html)**:
    - สร้าง Computed property `groupedTopCharges` เพื่อจับคู่หาข้อหาที่มีจำนวนเท่ากันมาจัดกลุ่มอยู่ในบรรทัดและลำดับเดียวกันเพื่อเรนเดอร์ลงในตารางคำอธิบายสไตล์ใหม่
    - เพิ่มความสูงสูงสุด (max-height: 280px) และเปิดสกรอลบาร์แนวดิ่งให้กับตารางคำอธิบายข้อหาเพื่อรองรับรายการที่เพิ่มขึ้นทั้งหมดได้อย่างเป็นระเบียบ
    - อัปเดตฟังก์ชัน `renderTopChargesChart` ให้จัดลำดับวงกลมโดยเทียบเคียง Ranks เดียวกันสำหรับข้อมูลที่เท่ากัน และขยายจานสีรองรับได้ถึง 30 สี


### 20. จัดรูปเลย์เอาต์คำอธิบายคดีและการเรียงลำดับเพศที่เข้มงวด (แก้ไขใน V142)
* **ความต้องการ**:
  - **จัดรูปข้อหาที่มีอันดับเท่ากัน**: ในกรณีที่ลำดับใดมีข้อหามากกว่า 1 ข้อหา ให้จัดข้อหาลำดับถัดไป (หลังเครื่องหมาย `,`) ขึ้นบรรทัดใหม่ และตัวเลข `(จำนวนคดี)` ให้แสดงผลเป็นตัวหนา
  - **จัดเรียงสถิติเพศ**: บังคับจัดลำดับหัวข้อเรียงจาก *"ชาย"*, *"หญิง"*, และ *"ไม่มีข้อมูล"* เสมอจากบนลงล่าง
* **การดำเนินการ**:
  - **การจัดการฝั่งหน้าบ้าน (Index.html & Scripts.html)**:
    - ปรับโครงสร้าง Vue Computed property `groupedTopCharges` ให้เก็บข้อหาเป็นรายการอาร์เรย์ย่อย (items) ในแต่ละกลุ่ม โดยมีพร็อพเพอร์ตี้ระบุเครื่องหมายจุลภาคต่อท้าย ยกเว้นไอเท็มสุดท้าย
    - ปรับเลย์เอาต์ HTML คำอธิบายด้วย CSS Flex ให้จัดวางตัวเลขลำดับ (เช่น `7.`) แยกคอลัมน์กับฝั่งข้อหา เพื่อให้การจัดแถวข้อความขึ้นบรรทัดใหม่ดูตรงกันและจัดชิดซ้ายอย่างสมดุล สวยงาม และทำการครอบข้อความจำนวนคดีด้วยแท็ก `font-weight: 700` เพื่อให้แสดงตัวหนา
    - เขียนตรรกะบังคับเรียงคีย์ (Key sorting logic) ของ `stats.genderCounts` บน JavaScript ให้อ้างอิงและล็อกตามอาร์เรย์ `["ชาย", "หญิง", "ไม่มีข้อมูล"]` ทุกครั้งที่มีการอัปเดตหรือฟิลเตอร์ข้อมูล เพื่อให้เบราว์เซอร์เรนเดอร์รายชื่อเพศตามลำดับดังกล่าวอย่างเคร่งครัด


### 21. เพิ่มสัญลักษณ์แสดงผลยอดรวมเยาวชนในการดูแลฝั่งผู้ให้คำปรึกษา (แก้ไขใน V143)
* **ความต้องการ**:
  - แสดงผลยอดรวมของเด็กและเยาวชนที่อยู่ในความดูแลของผู้ให้คำปรึกษาทั้งหมดในรูปแบบ `(X คน)` ต่อท้ายชื่อหัวข้อการ์ดแดชบอร์ดขวา *"จำนวนเด็กและเยาวชนที่อยู่ในความดูแลของผู้ให้คำปรึกษา ณ ปัจจุบัน"*
* **การดำเนินการ**:
  - **การจัดการฝั่งหน้าบ้าน (Index.html)**:
    - เพิ่มโค้ด HTML `<span>` ต่อท้ายหัวข้อการ์ดแดชบอร์ดขวา โดยผูกข้อมูลคำนวณผลรวมแบบไดนามิกด้วยฟังก์ชัน Vue `stats.counselorActiveCases.reduce((sum, item) => sum + item.count, 0)` แสดงผลรวมแบบเรียลไทม์ตามหมวดหมู่ตัวกรองได้อย่างแม่นยำ

### 22. จัดสัดส่วนตารางรายงาน ม.90 ให้สมดุลพอดีกับหน้ากระดาษ A4 (แก้ไขใน V168)
* **ความต้องการ**:
  - แก้ไขปัญหาตาราง ม.90 ในหน้าพรีวิวและหน้าเตรียมพิมพ์จริงล้นหน้าจอและล้นหน้ากระดาษ A4 (โดยเฉพาะหน้าที่ 3 ที่มีข้อมูลหนาแน่นถึง 43 แถว จนเบราว์เซอร์ตัดขึ้นหน้าใหม่เป็น 10 แผ่น และหน้าที่ 5 ที่มีพื้นที่โล่งมากเกินไป)
  - ปรับปรุงให้หน้าพรีวิวเหมือนหน้าเตรียมพิมพ์ของสรุป 100% โดยอิงกระดาษ A4 แนวตั้ง
* **การดำเนินการ**:
  - **จัดโครงสร้างกระจายข้อหาใหม่แบบสมดุล (5 หน้าเท่าเดิม)**:
    - **หน้าที่ 1**: ข้อหาประมวลกฎหมายอาญา (ข้อ 1 - 27) คงเดิม (~27 แถว)
    - **หน้าที่ 2**: ข้อหาประมวลกฎหมายอาญา (ข้อ 28 - 59) คงเดิม (~32 แถว)
    - **หน้าที่ 3**: ปรับลดข้อหาลง เหลือเฉพาะ พ.ร.บ. อาวุธปืนฯ และ พ.ร.บ. ยาเสพติดบางส่วน (ข้อ 80, 81-89, 60, 61, 62, 90-104) รวม ~28 แถว
    - **หน้าที่ 4**: แสดงข้อหา พ.ร.บ. อื่น ๆ (ข้อ 105-119, 63, 64-79) รวม ~32 แถว
    - **หน้าที่ 5**: แสดงข้อหาอื่นนอกเหนือจากบัญชีและตารางสรุปรวมท้ายรายงาน (ข้อ 120, 121-131 และแถวสรุปรวม 4 แถว) รวม ~16 แถว พร้อมกล่อง "จำนวนคดีที่ไม่อาจใช้มาตรการพิเศษ..." และลายเซ็นผู้จัดทำ/ผู้อำนวยการ
  - **ปรับแต่งสไตล์ CSS ให้กระชับพอดี (Compact & Responsive)**:
    - เพิ่มคลาส `.m90-sheet` ใน [Styles.html](file:///c:/Users/LENOVO/OneDrive/Cz/โปรแกรมศูนย์ให้คำปรึกษา/Styles.html) เพื่อกำหนดขนาดระยะขอบตาราง (padding: 8mm 12mm !important) และขนาดตัวอักษรของตารางโดยรวม (font-size: 11px !important) และเซลล์ต่าง ๆ ให้กระชับยิ่งขึ้น
    - ปรับปรุงความกว้างของคอลัมน์จากเดิม (ข้อหา 55% และคอลัมน์ตัวเลข 11.25%) เปลี่ยนเป็น **ข้อหา 48%** และ **คอลัมน์ตัวเลข 13%** เพื่อขยายพื้นที่ให้กับหัวข้อตาราง "ศาลไม่เห็นชอบด้วยกับแผนฯ/ปฏิบัติตามแผนฯครบถ้วน/ปฏิบัติตามแผนฯไม่ครบถ้วน" ซึ่งช่วยลดจำนวนการขึ้นบรรทัดใหม่ ทำให้ตารางไม่ยืดตัวในแนวดิ่ง
    - นำแถวสรุปยอดรวม (Totals) ย้ายไปวางต่อท้ายตารางหลักของหน้าที่ 5 เพื่อให้รายงานสมบูรณ์ตามลำดับโครงสร้างข้อมูล

### 23. พัฒนาระบบหน้าข้อมูลคดี (Case Info Module) และตารางย่อย (แก้ไขใน V224-228)
* **ความต้องการ**:
  - เพิ่มระบบจัดการ "ข้อมูลคดี" โดยละเอียดแยกต่างหากจากหน้ารายชื่อและหน้าประวัติเดิม
  - รองรับแท็บแยกตามประเภทมาตรา (มาตรา 73, มาตรา 90, มาตรา 132, คดีจากศาลอื่น, คดีอื่นๆ)
  - พัฒนา 3 ตารางย่อยจัดการข้อมูลประวัตินัดหมาย ประวัติเงื่อนไขที่ศาลกำหนด และประวัติกิจกรรมการเข้าร่วม
  - เพิ่มระบบพิมพ์ใบคุมคดี (Cover Sheet/Registration form) ที่สวยงาม จัดเรียงข้อมูลสองฝั่งเป็นระเบียบ
  - แปลงระบบการแสดงผลวันที่เป็นปี พ.ศ. (วว/ดด/พ.ศ.) ในหน้าเว็บทั้งหมด แต่เก็บข้อมูลหลังบ้านใน Google Sheets เป็นปี ค.ศ. (ISO format) อย่างปลอดภัย
* **การดำเนินการ**:
  - **สร้างระบบหลังบ้าน (Code.gs)**:
    - เพิ่มฟังก์ชัน `getCaseInfoModuleData(caseId)` ดึงประวัติข้อมูลคดี จำเลย การนัดหมาย เงื่อนไขศาล และกิจกรรมในคราวเดียว
    - เพิ่มฟังก์ชันบันทึกตารางย่อยอย่างปลอดภัย: `saveCaseAppointments`, `saveCaseConditions`, และ `saveCaseActivities` โดยตรวจสอบสิทธิ์ของบทบาทผู้ใช้
  - **สร้างอินเตอร์เฟซและแท็บหมวดหมู่ (Index.html)**:
    - ออกแบบแท็บโฟลเดอร์สำหรับแยกประเภทคดีด้วยสไตล์โมเดิร์น (Active gradient, hover effects)
    - สร้างโครงฟอร์มแสดงรายละเอียดคดีและข้อมูลเยาวชน แยก 2 ฝั่งซ้าย-ขวาอย่างสวยงาม
    - พัฒนาแบบฟอร์มจัดทำตารางย่อย 3 รายการ รองรับการเพิ่ม ลบ และอัปเดตแถวตารางในเครื่องก่อนกดบันทึกจริง
    - นำสไตล์ปุ่ม "พิมพ์เอกสารในคดี" และโครงสร้างแบบพิมพ์ A4 แนวตั้งสำหรับพิมพ์รายละเอียดใบคุมคดีอย่างเป็นทางการ
  - **การจัดการวันที่เป็น พ.ศ. และการป้อนข้อมูลด้วยตัวเลือกปฏิทิน (Scripts.html & Index.html)**:
    - พัฒนาฟังก์ชัน `formatBETextDate(isoDateStr)` และ `parseBETextDate(beDateStr)` แปลงระหว่าง พ.ศ. หน้าจอ และ ค.ศ. หลังบ้าน
    - ยูนิฟายการป้อนข้อมูลวันที่ทั้งหมดในระบบด้วยตัวคุม `be-date-picker-wrapper` ซึ่งใช้ตัวป้อนสไลด์ปฏิทิน Native Date Picker ซ่อนตัวอยู่เบื้องหลังการแสดงผลที่เป็นข้อความ พ.ศ. หน้าจอ ป้องกันปัญหาระบบเบราว์เซอร์ไม่รองรับปี พ.ศ. และทำให้ผู้ใช้งานคลิกเลือกปฏิทินได้อย่างแม่นยำ 100%
    - ผูกตัวแปร Vue และฟังก์ชันควบคุมสิทธิ์เพื่อปิดการบันทึก/แก้ไขหากผู้ใช้เป็นบทบาทผู้อำนวยการ (isDirector = true)

---

## การ Deploy และบันทึกโค้ด
- ✅ ดึงและอัปโหลดโค้ดผ่าน Clasp: `npx clasp push` (ส่งไฟล์ขึ้นระบบกูเกิลคลาวด์แล้ว)
- ✅ เผยแพร่เว็บแอปเวอร์ชันใหม่: อัปเดต Deployment ไปเป็น **Version 228** เรียบร้อยแล้ว (ID: `AKfycbxYiDojXu-dawRuV8uSbm4Hwkh2-gzaxFOzrT1Jc-jjAgTmoxmHoc0v36F0dPcEeZNc`)
- ✅ ตรวจสอบโครงสร้างแท็ก HTML และสคริปต์ JavaScript: ตรวจสอบความถูกต้องสมบูรณ์แบบ 100% ผ่านตัวเช็กไวยากรณ์ (Perfectly VALID)
- ✅ บันทึกความเปลี่ยนแปลงลง Git: คอมมิตและพุชโค้ดขึ้น GitHub เรียบร้อยแล้ว
